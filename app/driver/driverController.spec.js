'use strict';

describe('FunoChampionship.driver module', function() {

  beforeEach(module('FunoChampionship.driver'));

  describe('driver controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var driverController = $controller('driverController');
      expect(driverController).toBeDefined();
    }));

  });
});