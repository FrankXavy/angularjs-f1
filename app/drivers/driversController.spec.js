'use strict';

describe('FunoChampionship.drivers module', function() {

  beforeEach(module('FunoChampionship.drivers'));

  describe('drivers controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var driversController = $controller('driversController');
      expect(driversController).toBeDefined();
    }));

  });
});